---
title: P1 交接笔记（P1.13 完成，续 P1.15/P1.16）
date: 2026-07-27（更新）
seq: 2026-07-27-004
type: handoff
phase: P1.13 完成 → P1.15/P1.16
---

# P1 交接笔记

> squash 摘要模型有 enable_thinking 配置问题（不可用），故用此笔记做会话交接。新会话据此 + 设计文档 + git 历史续作。

## 项目与方向
llm-proxy 从"轻量本地代理"greenfield 重构为 **AxonHub 级本地网关**（除多租户）。TS/Node 22 + Hono + Drizzle + **PG-only** + Vitest + Playwright + tsup + Biome + citty。

## 关键文档（主仓库 /Users/mutallip/Desktop/project/agents/llm-proxy/docs/）
- 主计划 `plans/2026-07-27-002-master-axonhub-class-gateway-plan.md`
- 地基 ADR `adr/0004-pg-only-best-in-class-stack.md`
- **P1 设计** `plans/2026-07-27-003-feat-p1-protocol-core-design.md`（§3 IR 类型、§4 适配器边界、§6 PG schema SQL、§7.3 十条行为等价不变量）
- AxonHub 对标 `research/axonhub-analysis.md`

## 当前状态（P1.1–P1.13 完成）
- **工作 worktree**：`/Users/mutallip/Desktop/work/worktrees/gateway-p0-spike/Users/mutallip/Desktop/project/agents/llm-proxy`
- **分支** `muta/feat_gateway-p0-spike`，**HEAD** `8d76754`
- **typecheck 0 错误；测试 295 过 / 5 跳（known-limitation）/ 0 败**
- 已完成：P1.1 IR 类型 / P1.2 canonicalize / P1.3 接口 / P1.4–P1.10 适配器（inbound/outbound/response/stream/ccx/capture/abort）/ P1.11 pipeline 端到端 / P1.12 基础设施测试迁移 / **P1.13 行为等价黄金回归 + A+D 缺口修复**

## 测试金字塔（企业级分层）
```
golden/    5 文件  thinking-injection / translation-equivalence / translation-response / ccx-compat / stream-equivalence
unit/      7 文件  适配器单测（inbound/outbound/response）
integration/ 1     pipeline.test.ts（端到端 + mock 上游）
e2e/       1       Playwright 冒烟
legacy/   13       过渡（P1.12 迁移的基础设施测试，node:test→vitest）
```
测试接线 helper：`test/helpers/{route,translate,stream}.ts`（测试专用，非生产接口）。

## 25 处行为差异处理结论
- **A 类真实 bug 已修**：usage cache 透传（converters buildChatUsage 等 + 0 省略）/ stop_reason 从 output 推断（response-decode hasToolUseBlock）/ Chat reasoning_signature 顶层读取 / 流式 reasoning_text.delta（非 summary_text）+ choices[0].usage(Kimi) + cached_tokens=0 省略 / namespace 健壮性（null 防御）。
- **D 类 abort 已修**：`stream/abort.ts` 新增 `abortableIterator`（Promise.race 接 signal），三个 inbound decode 加可选 `signal` 参数，截断/abort 不发 [DONE]/finish。
- **B 类接受新行为（用户决策：不要历史包袱，企业级）**：内容统一 blocks 不 collapse 为字符串 / 相邻同 role 消息合并 / tool role 归一为 user / Anthropic input schema 严格化（强制 type:'message'）。
- **C 类 Computer Use 后置（known-limitation）**：computer action 顶层字段 / computer_use_preview→Chat / computer_call_output / coordinate-keys 重组 / pending_safety_checks / bash-text_editor 过滤。

## 两个小尾巴（续作时先处理）
1. **MCP probe 大小写**：`src/proxy/adapters/inbound/_shared.ts` 的 `isMcpProbeTool` 把 `name.startsWith(prefix)` 改为 `name.toLowerCase().startsWith(prefix)`，并更新 `test/golden/ccx-compat.test.ts` 期望（READ_MCP_RESOURCE 应被剥离）。
2. **abort 信号接入 pipeline**：`src/proxy/pipeline.ts` 的 stream inbound `decode(tappedIn)` 调用点未传 `params.signal`（适配器已支持 signal），需透传客户端断连信号。

## P1 剩余
- **P1.15 切流**：feature flag 新旧双跑 → 删旧 `legacy-src/proxy/translation.ts` + `stream-converter.ts`（新协议层已 295 测试验证，可切）。
- **P1.16 PG schema**（推荐先做）：`providers / provider_models / adapters / adapter_model_mappings / vision_settings / proxy_settings / usage_records` 落 PG（SQL 见 P1 设计 §6）+ ConfigStore 双写（当前配置仍是 YAML）。对齐 PG-only 决策，解锁 admin 对 provider/model 的 CRUD。
- **P1.17 文档**：协议层架构图 / IR 字段表 / adapter 边界 / 测试数对齐（CLAUDE.md 写 329 vs 实测）。

## 关键决策与规则（务必遵守）
- **PG-only**：复用现有 Docker `postgres` 容器（网络 `shared-net`，用户 `dev`），专用库 `llmproxy_dev`/`llmproxy_prod`；compose 只定义 app，接 external `shared-net`（不自带 PG）。
- **并发 subagent 必须独立 worktree 隔离**（已固化到 `~/.pi/agent/AGENTS.md`）——本次同 worktree 并发踩坑：一个 agent 的 `git checkout HEAD` 冲掉另两个的在制修改。
- **注释中文、标识符英文**（用户偏好，AGENTS.md「编码偏好」）。
- **旧接口不兼容**（不要历史包袱，不做 legacy facade），但**旧测试的行为规格保留为黄金回归**。
- 文件编辑审查 hook 偶尔摇摆/误报（曾就 normalizeMessages 内联↔恢复反复、误报未守卫/别名）——**以 typecheck + 测试为权威**，合理收敛即可，不无限迎合。
- 工具输出摘要模型（PI_DISTILL_MODEL）常报 summary-failed / squash 失败（enable_thinking 配置问题），但原始输出保留、命令正常执行。

## 子 worktree（可 gwt remove 清理）
`gateway-p1-{inbound,outbound,stream,ccx,pipeline,tests}`（已合并入基线）。

## 如何续作
1. `cd` 到工作 worktree（上面路径），`git status` 确认干净（HEAD 8d76754）。
2. 修两个小尾巴（MCP 大小写 + pipeline abort signal 透传）。
3. **P1.16 PG schema**（推荐先做）→ **P1.15 切流** → **P1.17 文档**。
4. 需派 subagent 时**各用独立 worktree**（gwt create + reset 到基线），别再同 worktree 并发。
