# 将模型能力归属到 Provider 模型

Provider 模型拥有上下文窗口、最大输出、输入模态及可复用的推理映射模板。Adapter 模型映射保留稳定的下游 source model ID，只存储目标 Provider 模型和一个统一推理等级；请求时 llm-proxy 通过目标模型的模板将等级解析为上游协议参数。这样可以将供应商特有的预算和模式从客户端与 Adapter 中隔离，同时让模板变更统一作用于所有引用它的模型。